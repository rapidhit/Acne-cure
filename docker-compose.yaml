services:
  backend:
    build: ./backend
    restart: unless-stopped
    volumes:
      - backend_data:/app/data
    expose:
      - "4000"

  frontend:
    build: ./frontend
    restart: unless-stopped
    depends_on:
      - backend
    expose:
      - "80"

volumes:
  backend_data:
